# STAR vertex-wise analysis

The numerical script compares `<hemi>.sphere.reg.backup` with the active STAR `<hemi>.sphere.reg`. The heatmap script converts the resulting MGH displacement file into a binned FreeSurfer annotation.
